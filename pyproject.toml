[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pasadi-bot"
version = "1.0.0"
dependencies = [
    "aiogram==3.13.1",
    "aiosqlite==0.21.0",
    "python-dotenv==1.0.1",
    "apscheduler==3.10.4",
]